/**
 * Placeholder — this component is removed by PR2·T11.
 * Kept temporarily as a no-op render so the build stays green between T6 and T11.
 */
export default function ContextWarningBar(_props: { conversationId: string; onNewChat?: () => void }) {
  return null;
}
